import { BibleStudyMember } from './bible-study-member.model';

describe('BibleStudyMember', () => {
  it('should create an instance', () => {
    expect(new BibleStudyMember()).toBeTruthy();
  });
});
